// browser.runtime.onMessage.addListener(handeMessage);

// function handeMessage(message) {
//     console.log(message);

//     if(message.command === "reload"){
//         //browser.tabs.reload({bypassCache: true});
//         //window.location.href = window.location.href;
//     }
// }